spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/tms
    username: tms_user
    password: tms_password
  jpa:
    open-in-view: false
logging:
  level:
    org:
      springframework:
        security: debug